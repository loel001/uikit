import React from 'react';
import { createStand } from '../../../stand/standConfig';

export const Stand = () => {
  return <div>Attachment3</div>;
};

export default createStand({
  title: 'Attachment',
  id: 'components/Attachment',
  group: 'components',
  version: '3.18.0',
  status: 'stable',
});
